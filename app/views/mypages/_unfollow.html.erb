<%= form_for(current_user.following_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= button_tag :type => "submit", id: "followBtn", :class =>"btn btn-default btn-s-md unfollow" do %>
      フォロー中
  <% end %>
<% end %>


  