<%= link_to "Wanted" , 
    want_path(current_user.wants.find_by(:shop_id => shop.id),
        shop_id: shop.id),
    :class => "btn-want btn btn-warning btn-sm",
    :id => "wantSubmit",
    :remote => true,
    :method => :delete %>