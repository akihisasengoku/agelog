<%= link_to "行きたい！" , 
    want_path(current_user.wants.find_by(:shop_id => shop.id),
        shop_id: shop.id),
    :class => "btn-wanted btn btn-warning btn-sm",
    :id => "wantSubmit",
    :remote => true,
    :method => :delete %>