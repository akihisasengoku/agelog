<%= link_to "Wanted" , 
    user_shop_path(current_user.wants.find_by(:shop_id => shop.id),
        shop_id: shop.id, type: "Want"),
    :class => "want-#{shop.id} btn-want btn btn-warning btn-sm",
    :remote => true,
    :method => :delete %>