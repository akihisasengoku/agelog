<%= link_to "アゲ↑" , 
    age_path(current_user.ages.find_by(:shop_id => shop.id),
        shop_id: shop.id),
    :class => "btn-aged btn btn-warning btn-sm",
    :id => "ageSubmit",
    :remote => true,
    :method => :delete %>